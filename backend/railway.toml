[build]
builder = "NIXPACKS"
buildCommand = "uv sync --frozen"

[deploy]
startCommand = "uv run hypercorn src.main:app --bind [::]:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10